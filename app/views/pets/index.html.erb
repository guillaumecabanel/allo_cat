<h1>All missing pets</h1>

<%= link_to 'Lost a pet?', new_pet_path %>

<ul>
	<% @pets.each do |pet| %>
		<li>
			<%= link_to pet_path(pet) do %>
				<%= pet.name %> [<%= pet.specy %>] - <%= pet.sign %>
			<% end %>
		</li>
	<% end %>
</ul>